{
  "name": "backend-graphql",
  "version": "1.0.0",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "prisma generate && tsc",
    "pull": "prisma db pull",
    "generate": "prisma generate"
  }
}
